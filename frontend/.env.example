# Your FastAPI backend (the my-rag-cahtbot app), publicly reachable.
VITE_API_BASE_URL=http://18.224.181.131

# Powabase project URL (Studio -> Connect modal).
VITE_POWABASE_URL=https://YOUR-PROJECT.p.powabase.ai

# Powabase Anon Key (Studio -> Connect modal). Safe to expose in a frontend
# build -- this is NOT the Service Role key, never use that one here.
VITE_POWABASE_ANON_KEY=your-anon-key-here
